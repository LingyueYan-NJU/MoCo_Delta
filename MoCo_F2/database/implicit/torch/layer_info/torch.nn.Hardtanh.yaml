api: torch.nn.Hardtanh(min_val=- 1.0, max_val=1.0, inplace=False, min_value=None,
  max_value=None)
constraints:
  inplace:
    descp: can optionally do the operation in-place.
    default: false
    dtype: torch.bool
  max_val:
    descp: maximum value of the linear region range.
    default: 1.0
    dtype: torch.float
  min_val:
    descp: minimum value of the linear region range.
    default: -1.0
    dtype: torch.float
descp: Applies the HardTanh function element-wise.
inputs:
  optional:
  - min_val
  - max_val
  - inplace
  required: []
