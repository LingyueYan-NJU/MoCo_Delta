api: torch.nn.ReflectionPad1d(padding)
constraints:
  padding:
    dtype: int
    structure:
    - integer
    - tuple
    shape: 1
    range:
    - 1
    - 8
descp: Pads the input tensor using the reflection of the input boundary.
inputs:
  optional: []
  required:
  - padding
