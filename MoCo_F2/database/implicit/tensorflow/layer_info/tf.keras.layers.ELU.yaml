api: tf.keras.layers.ELU(alpha=1.0, **kwargs)
constraints:
  alpha:
    default: 1.0
    descp: Scale for the negative factor.
    dtype: float
descp: Exponential Linear Unit.
inputs:
  required: []
  optional:
  - alpha
