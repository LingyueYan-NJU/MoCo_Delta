api: torch.nn.ZeroPad2d(padding)
constraints:
  padding:
    dtype: int
    structure:
    - integer
    - tuple
    shape: 4
    range:
    - 1
    - 8
descp: Pads the input tensor boundaries with zero.
inputs:
  optional: []
  required:
  - padding
