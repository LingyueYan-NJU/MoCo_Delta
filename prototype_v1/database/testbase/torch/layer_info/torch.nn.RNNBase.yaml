api: torch.nn.RNNBase(class , mode, input_size, hidden_size, num_layers=1, bias=True,
  batch_first=False, dropout=0.0, bidirectional=False, proj_size=0, device=None, dtype=None)
constraints: {}
descp: Resets parameter data pointer so that they can use faster code paths.
inputs:
  optional: []
  required: []
