api: torch.nn.ReplicationPad3d(padding)
constraints:
  padding:
    dtype: int
    structure:
    - integer
    - tuple
    shape: 6
    range:
    - 1
    - 8
descp: Pads the input tensor using replication of the input boundary.
inputs:
  optional: []
  required:
  - padding
