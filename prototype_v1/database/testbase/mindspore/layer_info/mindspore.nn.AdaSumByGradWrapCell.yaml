api: mindspore.nn.AdaSumByGradWrapCell(optimizer)
descp: Enable the adasum in “auto_parallel/semi_auto_parallel” mode. The implementation of the Adaptive Summation (AdaSum) algorithm is calculated by gradients.
constraints:
  optimizer:
    descp: optimizer (Union[Cell]) – Optimizer for updating the weights. The construct
      function of the optimizer requires only one input.
    default: null
    dtype:
    - union[cell]
inputs:
  optional: []
  required: []
