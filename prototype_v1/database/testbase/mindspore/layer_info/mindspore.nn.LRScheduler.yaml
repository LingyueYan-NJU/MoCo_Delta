api: mindspore.nn.LRScheduler(optimizer, last_epoch=- 1, verbose=False)
descp: Basic class of learning rate schedule.
constraints:
  optimizer:
    descp: optimizer (mindspore.nn.optim_ex.Optimizer) – The optimizer instance.
    default: null
    dtype:
    - mindspore.nn.optim_ex.optimizer
  last_epoch:
    descp: 'last_epoch (int, optional) – The epoch/step number. Default: -1.'
    default: -1
    dtype:
    - int
    structure:
    - single
    range: null
  verbose:
    descp: 'verbose (bool, optional) – Whether to print lr information. Default: False.'
    default: False
    dtype:
    - bool
inputs:
  optional:
  - last_epoch
  - verbose
  required:
  - optimizer
