api: mindspore.nn.MultiLabelSoftMarginLoss(weight=None, reduction='mean')
constraints:
  reduction:
    default: mean
    descp: "reduction (str) \u2013 Specifies which reduction to be applied to the\
      \ output. It must be one of 'none' , 'mean' , and 'sum' , meaning no reduction,\
      \ reduce mean and sum on output, respectively. Default: 'mean' ."
    dtype:
    - str
    enum:
    - none
    - mean
    - sum
  weight:
    default: None
    descp: "weight (Union[Tensor, int, float]) \u2013 The manual rescaling weight\
      \ given to each class. Default: None ."
    dtype:
    - int
    - float
    range: null
    structure:
    - single
descp: Calculates the MultiLabelSoftMarginLoss.
inputs:
  optional:
  - reduction
  required: []
