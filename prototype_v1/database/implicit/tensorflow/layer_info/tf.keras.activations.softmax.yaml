api: tf.keras.activations.softmax(x, axis=-1)
constraints:
  axis:
    default: '-1'
    descp: Integer, axis along which the softmax normalization is applied.
    dtype:
    - int
  x:
    descp: Input tensor.
descp: Softmax converts a vector of values to a probability distribution.
inputs:
  optional:
  - axis
  required:
  - x
