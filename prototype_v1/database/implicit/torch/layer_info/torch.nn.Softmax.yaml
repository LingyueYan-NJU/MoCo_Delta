api: torch.nn.Softmax(dim=None)
constraints:
  dim:
    dtype: int
    structure:
    - integer
    shape: 1
    range:
    - -1
    - 1
descp: Applies the Softmax function to an n-dimensional input Tensor rescaling them
  so that the elements of the n-dimensional output Tensor lie in the range [0,1] and
  sum to 1.
inputs:
  optional:
  - dim
  required: []
