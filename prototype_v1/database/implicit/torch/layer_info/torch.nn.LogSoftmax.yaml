api: torch.nn.LogSoftmax(dim=None)
constraints:
  dim:
    dtype: int
    structure:
    - integer
    shape: 1
    range:
    - 1
    - 2
descp: "Applies the log\u2061(Softmax(x))\\log(\\text{Softmax}(x))log(Softmax(x))\
  \ function to an n-dimensional input Tensor. The LogSoftmax formulation can be simplified\
  \ as:"
inputs:
  optional:
  - dim
  required: []
