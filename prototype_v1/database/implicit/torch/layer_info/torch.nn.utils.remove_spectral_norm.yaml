api: torch.nn.utils.remove_spectral_norm(module, name='weight')
constraints:
  module:
    descp: containing module
    dtype:
    - module
  name:
    descp: name of weight parameter
    dtype:
    - str
    enum: null
descp: Removes the spectral normalization reparameterization from a module.
inputs:
  optional:
  - name
  required:
  - module
