api: mindspore.nn.Softmax(axis=- 1)
constraints:
  axis:
    default: -1
    descp: "axis (Union[int, tuple[int]]) \u2013 The axis to apply Softmax operation,\
      \ if the dimension of input x is x.ndim, the range of axis is [-x.ndim, x.ndim),\
      \ -1 means the last dimension. Default: -1 ."
    dtype:
    - int
    range: null
    shape: null
    structure:
    - single
    - tuple
descp: Softmax activation function, which is a two-category function mindspore.
inputs:
  optional:
  - axis
  required: []
