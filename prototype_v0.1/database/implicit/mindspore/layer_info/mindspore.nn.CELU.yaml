api: mindspore.nn.CELU(alpha=1.0)
constraints:
  alpha:
    default: 1.0
    descp: "alpha (float) \u2013 The (alpha) value for the Celu formulation. Default:\
      \ 1.0 ."
    dtype:
    - float
descp: Continuously differentiable exponential linear units activation function.
inputs:
  optional:
  - alpha
  required: []
