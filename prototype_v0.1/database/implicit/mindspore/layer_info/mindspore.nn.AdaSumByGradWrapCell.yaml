api: mindspore.nn.AdaSumByGradWrapCell(optimizer)
constraints:
  optimizer:
    default: null
    descp: "optimizer (Union[Cell]) \u2013 Optimizer for updating the weights. The\
      \ construct function of the optimizer requires only one input."
    dtype:
    - union[cell]
descp: "Enable the adasum in \u201Cauto_parallel/semi_auto_parallel\u201D mode. The\
  \ implementation of the Adaptive Summation (AdaSum) algorithm is calculated by gradients."
inputs:
  optional: []
  required: []
