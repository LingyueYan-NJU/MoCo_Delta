api: tf.keras.layers.Minimum(**kwargs)
constraints:
  '**kwargs':
    descp: Standard layer keyword arguments.
descp: Layer that computes the minimum (element-wise) a list of inputs.
inputs:
  optional: []
  required:
  - '**kwargs'
