api: mindspore.nn.ConstantPad1d(padding, value)
descp: Using a given constant value to pads the last dimensions of input tensor.
constraints:
  padding:
    descp: padding (Union[int, tuple]) – The padding size to pad the last dimension
      of input tensor. If is int, uses the same padding in both boundaries of input’s
      last dimension. If a 2-tuple, uses (padding_0, padding_1) to pad. If the input
      is x, the size of last dimension of output is (padding_0 + x.shape[-1] + padding_1).
      The remaining dimensions of the output are consistent with those of the input.
    default: null
    dtype:
    - int
    structure:
    - single
    - tuple
    shape: 2
    range: null
  value:
    descp: value (Union[int, float]) – Padding value.
    default: null
    dtype:
    - int
    - float
    structure:
    - single
    range: null
inputs:
  optional: []
  required:
  - padding
  - value
