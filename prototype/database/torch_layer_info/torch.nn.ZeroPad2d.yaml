api: torch.nn.ZeroPad2d(padding)
constraints:
  padding:
    dtype: int
    range:
    - 1
    - 8
    shape: 4
    structure:
    - integer
    - tuple
descp: Pads the input tensor boundaries with zero.
inputs:
  optional: []
  required:
  - padding
