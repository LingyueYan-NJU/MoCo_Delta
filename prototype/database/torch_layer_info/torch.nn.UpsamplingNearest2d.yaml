api: torch.nn.UpsamplingNearest2d(size=None, scale_factor=None)
constraints:
  scale_factor:
    descp: multiplier for spatial size.
    dtype: torch.float
  size:
    default: None
    descp: output spatial sizes
    dtype: int
    range:
    - 1
    - 8
    shape: 2
    structure:
    - integer
    - tuple
descp: Applies a 2D nearest neighbor upsampling to an input signal composed of several
  input channels.
inputs:
  optional:
  - size
  - scale_factor
  required: []
