api: torch.nn.ReplicationPad2d(padding)
constraints:
  padding:
    dtype: int
    range:
    - 1
    - 8
    shape: 4
    structure:
    - integer
    - tuple
descp: Pads the input tensor using replication of the input boundary.
inputs:
  optional: []
  required:
  - padding
