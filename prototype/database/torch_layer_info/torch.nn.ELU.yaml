api: torch.nn.ELU(alpha=1.0, inplace=False)
constraints:
  alpha:
    default: 1.0
    descp: the
    dtype: torch.float
  inplace:
    default: false
    descp: can optionally do the operation in-place.
    dtype: torch.bool
descp: 'Applies the Exponential Linear Unit (ELU) function, element-wise, as described
  in the paper: Fast and Accurate Deep Network Learning by Exponential Linear Units
  (ELUs).'
inputs:
  optional:
  - alpha
  - inplace
  required: []
