api: prelu
constraints:
  init:
    default: 0.25
    descp: the initial value of
    dtype: torch.float
  num_parameters:
    default: 1
    descp: number of
    dtype: int
descp: 'Applies the element-wise function:'
inputs:
  optional:
  - num_parameters
  - init
  required: []
